type Query {
	website: [Website]!
}

type Mutation {
	createWebsite(url: String, title: String): Website
}

type Subscription {
	website: Website!
}

type Website {
	id: ID!
	url: String!
	title: String
}
